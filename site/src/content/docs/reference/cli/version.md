---
title: "lazydap version"
description: "Print the lazydap and protocol versions"
---

:::note[Generated page]
From `lazydap version --help`. To change it, change the clap definition in [`crates/daemon/src/cli/`](https://github.com/planetaryescape/lazydap/tree/main/crates/daemon/src/cli) — the site rebuilds from the binary.
:::

Print the lazydap and protocol versions

```text
lazydap version [OPTIONS]
```

## See also

- [CLI overview](/reference/cli/) — every command in one list
- [JSON output](/reference/json-output/) — the shape of what comes back
- [Errors and exit codes](/reference/errors/) — what to do when it fails
