---
title: "lazydap scopes"
description: "Show the variable scopes of a frame"
---

:::note[Generated page]
From `lazydap scopes --help`. To change it, change the clap definition in [`crates/daemon/src/cli/`](https://github.com/planetaryescape/lazydap/tree/main/crates/daemon/src/cli) — the site rebuilds from the binary.
:::

Show the variable scopes of a frame

```text
lazydap scopes [OPTIONS]
```

## Options

| Flag | Description |
| --- | --- |
| `--frame <FRAME>` | Which frame. Defaults to the top one |

Plus the [options every command takes](/reference/cli/#options-every-command-takes).

## See also

- [CLI overview](/reference/cli/) — every command in one list
- [JSON output](/reference/json-output/) — the shape of what comes back
- [Errors and exit codes](/reference/errors/) — what to do when it fails
