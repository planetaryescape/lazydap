---
title: "lazydap tui"
description: "Open the terminal UI. This is also what bare `lazydap` does on a terminal"
---

:::note[Generated page]
From `lazydap tui --help`. To change it, change the clap definition in [`crates/daemon/src/cli.rs`](https://github.com/planetaryescape/lazydap/blob/main/crates/daemon/src/cli.rs), then run `npm run generate` in `site/` and commit the result. CI fails if this page and the binary disagree.
:::

Open the terminal UI. This is also what bare `lazydap` does on a terminal

```text
lazydap tui [OPTIONS]
```

## See also

- [CLI overview](/reference/cli/) — every command in one list
- [JSON output](/reference/json-output/) — the shape of what comes back
- [Errors and exit codes](/reference/errors/) — what to do when it fails
