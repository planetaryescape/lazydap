---
// Site-wide pre-release notice. Starlight's stock Banner renders only when a
// page sets `banner` in its frontmatter; this override renders on every page
// instead. Remove the `Banner` entry from astro.config.mjs when v0.1.0 is
// tagged and binaries are published.
---

<div class="sl-banner">
  <strong>Pre-release.</strong> <code>v0.1.0</code> is not tagged and there are no published
  binaries yet. <a href="/getting-started/install/">Install from source</a>. Commands on this site
  were run against the current <code>main</code>.
</div>
