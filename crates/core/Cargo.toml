[package]
name = "lazydap-core"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
homepage.workspace = true
keywords.workspace = true
categories.workspace = true
description = "lazydap's domain types, errors and the DebugAdapter trait. No I/O"
publish = false

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
uuid = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
# Only to prove the persisted shapes survive a hand-edited `.lazydap/state.toml`
# (D006). `lazydap-store` owns the actual file.
toml = { workspace = true }
